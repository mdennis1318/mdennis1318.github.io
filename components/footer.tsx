import React from 'react';

const Footer: React.FC = () => {
    return (
        <footer>
            <p className="footer-text">💻 ~ {new Date().getFullYear()} Matthew Dennis Portfolio ~ 💻</p>
        </footer>
    );
};

export default Footer;
